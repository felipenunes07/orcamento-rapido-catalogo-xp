
import React from 'react';
import HomePage from './HomePage';

// Render the HomePage component directly instead of redirecting
const Index = () => {
  return <HomePage />;
};

export default Index;
